[pytest]
testpaths = tests

# tests/manual holds scripts that need a real account, the network or a
# person at the keyboard -- see tests/manual/README.md. Their filenames
# already avoid the test_ prefix; this makes it explicit.
norecursedirs = manual

filterwarnings =
    ignore::DeprecationWarning
